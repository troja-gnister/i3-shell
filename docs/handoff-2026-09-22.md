# Phase 2B pause handoff — 2026-09-22

> **Superseded.** Work resumed the same day and Phase 2B Task 10 is complete (`4057afa`). This
> document is kept as the record of that pause; for current state read [PROJECT.md](../PROJECT.md),
> the [plan's execution record](superpowers/plans/2026-09-22-phase-2b-integration.md) and the
> unchecked [Phase 2 acceptance checklist](acceptance/phase-2.md).

The user asked: “please pause. update all the docs. then give me a handoff message to another session”. Implementation **was** paused at this point, before Task 10; it resumed the same day at the user's request and Phase 2B is now complete. Everything below describes the state at the pause and is retained as history only.

## Repository and installed build

- Checkout: `/var/home/troja/Dev/i3-shell`; branch **`phase-2b`**. Work in this checkout, not a worktree: installation symlinks its `dist/`.
- Last checkpoint before this documentation update: **`7fc67d3`**. Latest production change: **`bdf0cb8`**; harness-only critical-log correction: **`c8b64cd`**. The handoff commit changes documentation only; obtain its id with `git log -1`.
- Phase 2B base / whole-branch review base: **`b6fe8cc`**. `main` remains there; `origin/main` is `568855c`. Remote: `git@github.com:troja-gnister/i3-shell.git`. No Phase 2B merge/push is authorized or performed.
- **Release `make install` passed after the last native runs.** The install symlink is `~/.local/share/gnome-shell/extensions/i3-shell@troja` → this checkout's `dist/`. Debug interface XML and methods are absent. The user was not logged out/in; the loaded live module revision is unclaimed.
- No native command/session or pending build remains. Task 10's worker was stopped after reading its brief and one skill; it made **no edits**, ran no verification, and wrote no report.

## Read and resume

1. [PROJECT.md](../PROJECT.md), then the [approved Phase 2B plan](superpowers/plans/2026-09-22-phase-2b-integration.md), [binding spec](superpowers/specs/2026-09-20-i3-shell-design.md), and [Phase 1 carry-forward](superpowers/plans/2026-09-21-phase-1-carry-forward.md).
2. Local ignored workspace `.superpowers/sdd/2026-09-22-phase-2b-integration/`: `progress.md`, `constraints.md`, `contracts.md`, task briefs/reports/reviews and diff packages. Preserve it while paused. If another checkout lacks it, use the committed execution record and this handoff; reconstruct only the outstanding Task 10 context.
3. **Tasks 1–9 are complete and independently reviewed. Start with Task 10 on authorized resumption.** Its existing brief has the full requirements; regenerate it after the documentation update with the SDD `task-brief` script. Record the current HEAD as Task 10's new baseline. Do not restart earlier tasks or repeat the resolved startup investigation.
4. Preserve the Superpowers subagent workflow: one implementer per task, per-task review, then whole-branch review. Controller owns git mutations after worker FINAL. Workers never spawn reviewers or child agents. Use actual authoring-model commit trailers.
5. Controller also owns **all builds, native harness runs and installs**. Workers request exact commands and freeze affected files until completion. Do not replace a harness while an invocation is unwinding. A pending approval is not a running command.

## Verified at the pause

| Check | Result |
|---|---|
| Full unit suite after native lifetime fix | **371/371 tests, 36 files** |
| Native lifecycle/tracker regressions | **10/10**, with three initial failing regressions |
| Both TypeScript programs and Layer 0 | Passed |
| GTK smoke | Passed; private socket/bus/runtime, close before first frame, normal adoption/removal, fixture validation |
| Retained Phase 1 native checks | Passed A1–A5/A7, including exact 50/50 → 60/40 actual frames, empty Entry text, resize 11/default 65 grabs, pills, exec, reload and lock recovery |
| Cleanup critical-log correction | Actual cleanup driver: RED 10/14 → GREEN **14/14**; shell syntax/diff passed |
| Task 9 review | Initial fixture-log gate finding fixed; scoped re-review clean |
| Release install / Debug exclusions / symlink | Passed |

Final native logs on this machine: smoke `/tmp/i3-shell-nested.vde8dR`; Phase 1 `/tmp/i3-shell-nested.Ipv6gX`; controller output `/tmp/i3-shell-task9-phase1-controller.log`. These `/tmp` files are evidence aids, not durable dependencies. Complete evidence and chronology are in `task-9-report.md` and the committed plan's execution record.

The matcher-only `c8b64cd` change did not require rebuilding unchanged production code or rerunning native scenarios. Both final native log pairs were inspected; the missed critical domains were absent. Ancillary a11y, GJS NetworkManager, GDM and portal warnings remain disclosed. Do not claim pristine host-service output.

## What Task 10 still must do

- Create `test/integration/phase2-checks.py` with independent rectangle expectations for A8–A14: nested split/focus/move/wrap, parent movement/layout/acknowledgement, width/height resizing, tabbed/stacked visibility, transient/modal/fixed/floating cases, parent kill/transfer without workspace following, nested reload/restart and lifecycle states.
- Exercise fullscreen exit, unminimize and caught maximization; test a minimum-size refusing client without unbounded retries. Reproduce production failures in focused unit tests before making a source fix.
- Run actual two-output reconfiguration through private Mutter DisplayConfig; preserve stable monitor ids and membership. If this headless backend rejects temporary output removal, document that specific limitation and leave physical monitor acceptance unchecked; do not fake a successful event.
- Add initially-disabled harness support for true before/after settings restoration, repeated enable/adoption, and private Control-name conflict. Core keys/tiling must survive the name conflict without displacing its owner.
- Add `test/integration/run.sh` and update `npm run test:integration` to build TEST, run all fresh scenarios and restore release on exit. The current npm script runs **only Phase 1** and requires a test build first.
- Create `docs/acceptance/phase-2.md` with exact product commit/build/date and **unchecked** A8–A14 live boxes. Current pause docs are an interim status update, not final delivery evidence. Update them again with actual Task 10 outcomes.
- Complete Task 10 review, final unit/typecheck/Layer 0/tree lint/integration checks, release `make install`, Debug/symlink verification, and whole-branch review over `b6fe8cc..HEAD`. Do not rerun unchanged passing checks without a reason.
- Whole-branch review gets both deferred Minors: normalized ignored-type labels do not independently exercise the native enum mapper (`classify.test.ts:30`), and successful native logs contain the disclosed ancillary warnings. One complete final fix wave and one scoped re-review, then explicit adjudication per the skill. Preserve all rulings before deleting only this plan's scratch workspace after completion.
- Keep `phase-2b` until the user requests integration. A8–A14 live acceptance is still the user's future walk. Do not start Phase 3 automatically.

## Native facts that prevent repeated investigation

- GNOME Shell 50.5 / Mutter 18 / Fedora Silverblue 44, Wayland. New extension code needs logout/login; disable/enable alone reuses the loaded module. Never log the user out.
- The private harness uses `--no-x11`: the pre-integration baseline also stalled in nested Xwayland startup, while changing only this flag made D-Bus, timers and teardown responsive. Required GTK fixtures are native Wayland. Xwayland clients and visible mode are unverified; two-monitor scenarios have not run.
- Seed private `data/gnome-shell/update-check-50` to avoid the first-version network updater. This marker **did not** fix the startup stall by itself; do not repeat that disproven explanation.
- GTK fixture readiness: wait Shell ready, press Escape, poll actionMode 1 before normal creation. Its first frame was delayed in the initial overview. Production waits for a real first frame; preexisting mapped windows are adopted directly.
- Mutter order: `unmanaging` → actor removal → focus changes → workspace cleared → `unmanaged`. Native lifetime guards exclude retiring windows and filter the last safe MRU order until teardown ends. Pending actor destruction invalidates its disposer. Preserve these tested boundaries.
- Shell Extensions interface is on `/org/gnome/Shell`, bus `org.gnome.Shell`, interface `org.gnome.Shell.Extensions`. Sandbox `ps` can hide escalated native PIDs; use appropriate process inspection before diagnosing death.
- Fixture uses `GLibUnix.signal_add`; `GTK_A11Y=none` is fixture-only. `GetWindows.rect` is the native Mutter frame; fixture `Size` is GTK client allocation, used as fallback only in the deliberate name-conflict scenario.
- Release checks search exact XML `name="org.i3shell.Debug"` and `PressKey`, `SimulateSessionMode`, `Relayout`. An inert `if (false)` log/comment can still mention `org.i3shell.Debug` without exporting it.

## Rulings made, in order

1. Defer complex nested reload/pending-split and selected-parent regressions from Task 5 to Task 6, where command dispatch exists. Cost if wrong: discovering defects one task later. **Closed by Task 6's clean review.**
2. Use `Meta.TabList.NORMAL_ALL_MRU` rather than the spec's old `NORMAL` example to preserve true per-workspace MRU including minimized windows. Cost if wrong: different eligible-window set/order; native adoption/minimize proof remains in Task 10.
3. Include Engine's copied committed pill state in Task 7 instead of calculating it again in ControlObject. Cost if wrong: the additional public field/storage may need adjustment. Task 7 reviewed clean.
4. Seed the disposable GNOME 50 update marker. Cost if wrong: bypassing an unrelated future startup check; GNOME's network updater is not tested. The initial stall hypothesis was disproven and corrected.
5. Launch the private GTK compositor with `--no-x11`. Cost: automated coverage excludes Xwayland clients. This resolved the verified baseline startup stall.
6. Expand Task 9 to fix native lifetime defects found by real GTK teardown. Cost if wrong: removal timing/MRU behavior could regress. Focused ordering tests, native smoke and task review passed.

## Boundaries to preserve

Only Engine mutates Tree and every mutation/geometry write goes through commit. Commands use engine selection and opaque WindowIds. Exactly four forced generations: fullscreen exit, unminimize, monitors-changed, completion of engine unmaximize; workspace/work-area/unlock use normal reconciliation. One corrective reapply per expected generation. Splash is ignored; fixed-size normal windows float. Tabbed/stacked children share parent rectangles.

Borders/tab bars are Phase 3. Rules, urgent pills, cross-monitor navigation, workspace back-and-forth and full dock/lid fidelity are Phase 4. Target arrangements remain laptop alone and docked with external output(s)/lid closed; ask scaling/resolutions only when Phase 4 planning starts. Consult the user's i3-display-manager/i3-lid-sleep projects then. Marks and scratchpad remain outside v1.
